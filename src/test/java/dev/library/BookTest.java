package dev.library;


import org.junit.Test;
import static org.junit.Assert.*;

public class BookTest {
    

    //check late fee
    /*@Test
    public void checkLateFee_whenDaysTenth_returnsSixty() {
        Book book = new Book("TestName", "TestGenre", "TestAuthor");
        book.borrowBook();
            for (int i = 0; i < 10; i++) {
            book.advanceDay();
            }
        assertEquals(60, book.checkLateFee());
    }
    @Test
    public void checkLateFee_whenDaysEqualSeven_returnsZero() {
        Book book = new Book("TestName", "TestGenre", "TestAuthor");
        book.borrowBook();
            for (int i = 0; i < 7; i++) {
            book.advanceDay();
            }
        assertEquals(0, book.checkLateFee());
    }
    @Test
    public void checkLateFee_whenDaysEight_returnsTwenty() {
        Book book = new Book("TestName", "TestGenre", "TestAuthor");
        book.borrowBook();
            for (int i = 0; i < 8; i++) {
            book.advanceDay();
            }
        assertEquals(20, book.checkLateFee());
    }*/

    



}
